export type IParams = {
  chanelName: string,
  role: string,
  nama: string
}

export type IPropsOnboard = {
  chanel: string | null,
  state: any,
  role: string | null,
  name: string | null,
  actCall: Function
}